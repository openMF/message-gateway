apply plugin: 'war'

configurations {
    providedRuntime
}

war {
    archiveBaseName = 'message-gateway'
    archiveVersion = '0.0.1'
}

dependencies {
    providedRuntime("org.springframework.boot:spring-boot-starter-tomcat")
}
